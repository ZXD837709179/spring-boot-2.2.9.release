#yml优先级高于property
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8
    username: root
    password: root123456
    #druid的具体参数也可以不配置在datasource下，见druidconfig类
    druid:
      max-active: 10
      min-idle: 2
mydruid:
  config:
    initial-size: 2